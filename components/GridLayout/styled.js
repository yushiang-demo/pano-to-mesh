import styled from "styled-components";

export const Wrapper = styled.div`
  display: inline-flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
  padding: 10px;
  width: 50%;
  max-height: 80dvh;
  overflow: auto;
`;
